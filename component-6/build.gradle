plugins {
    id 'net.researchgate.release' version '3.0.2'
}

release {
    failOnCommitNeeded = false
    buildTasks = ['prerelease']
    tagTemplate = '${name}-${version}'
}

task prerelease {
    dependsOn ':component-1:printName'
}
