rootProject.name = 'component-1'

include ':sample-java-project-1'
include ':sample-java-project-2'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("$rootDir/../libs.versions.toml"))
        }
    }
}
